#include <iostream>
#include <math.h>

using namespace std;

int main()
{
    double cost, total=0;
    int tip, tax;

    cin>>cost>>tip>>tax;

    total+=cost;
    total+=tip*cost/100;
    total+=tax*cost/100;

    cout<<"The total meal cost is "<<round(total)<<" dollars.";

return 0;
}
